import User from "./models/User"

const userRegister = async () => {
    try {
        const newUser = new User({
            name: 
        })
    } catch () {
        
    }

}

export default userSeed